<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title>Index</title>
  </head>
  <body>
    <div class="header">
      <h1><%= title %></h1>
    </div>
    <% for (let i = 0; i < messages.length; i++) { %>
    <div class="message">
      <h2 class="name"><%= messages[i].username %></h2>
      <p class="text"><%= messages[i].message %></p>
      <p class="date"><%= messages[i].created_at %></p>
      <a href="/message/<%= messages[i].id %>">Details</a>
    </div>
    <% } %>
    <div class="add">
      <a href="/new">Add New Message</a>
    </div>
  </body>
</html>
